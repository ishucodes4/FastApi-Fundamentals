commands:
pip install fastapi
pip install uvicorn (for creating webserver of created api)

create a python file in VS Code inside FastApi Folder named myapi.py.
copy it address and paste in the command prompt to make sure that we are inside that directory :)
-> cd C:\Users\Dell\OneDrive\Desktop\fastapi(press enter)
-> enter dir(enter)

